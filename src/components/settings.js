export const Settings = {
  switches: {
    johnMode: false,
    ianMode: false,
    showAnimation: true,
  }
}

export const SettingsLabels = {
  johnMode: "John Mode",
  ianMode: "Ian Mode",
  showAnimation: "Show Animations"
}
